# v0.1.0

Initial public release of the ZMK Behavior Dynamic Macro module.

## Compatibility

- Module release: `v0.1.0`
- Module commit: annotated `v0.1.0` tag target. The implementation baseline reviewed before this release-note update was `7a702784bdeab33b3a2775cb756f6227a73417f7`.
- Required ZMK revision: `904c9aec8822d79149d42c8a9a77e8828eb08f5a`
- main-canary result: the tag workflow runs current ZMK `main` as a non-blocking canary. The fixed ZMK revision above is the supported compatibility target for `v0.1.0`; any canary regression is handled as follow-up compatibility work rather than a `v0.1.0` release blocker.

## Automated gates

The GitHub Release job depends on the automated gates below and only publishes after they pass on the annotated tag.

- [x] `Nix checks` gate is required by `.github/workflows/ci.yml` before release publication.
- [x] `ZMK compatibility (fixed)` gate is required by `.github/workflows/ci.yml` before release publication and validates ZMK revision `904c9aec8822d79149d42c8a9a77e8828eb08f5a`.
- [x] `Consumer manifest` gate is required by `.github/workflows/ci.yml` before release publication and validates west-manifest consumption of the tagged module.
- [x] CI records resolved west project revisions via the `Fetch ZMK west modules` step in the ZMK compatibility job.

## Hardware persistence evidence

- Test keyboard and controller: Kinesis Advantage 360 Pro split keyboard (`adv360pro_left` central and `adv360pro_right` peripheral, nRF52840)
- Central firmware artifact/checksum: maintainer validation build for the left/central half, SHA-256 `01a3375b5cfb7bbe5abcb931bd5757e22b25f20053733870563d5fdf8a06c15a`
- Peripheral firmware artifact/checksum: maintainer validation build for the right/peripheral half, SHA-256 `dc6806b4357586bf54d3cff79a001f2e39aed34923132d3d29aaead549d516c0`
- Tester/date: Mark, 2026-07-10

- [x] Record `A B C`, stop, play, and verify exactly `A B C`: user-attested hardware test passed on the Kinesis Advantage 360 Pro validation build above.
- [x] Reboot/power-cycle and verify the macro still plays when persistence is enabled: user-attested hardware test passed; slot 0 still replayed `A B C` after reboot/power-cycle.
- [x] Clear the slot, reboot, and verify it remains empty: user-attested hardware test passed; cleared slot remained empty after reboot/power-cycle.
- [x] Cancel a replacement recording and verify the old slot is restored when `CONFIG_ZMK_ZBDM_CANCEL_RESTORE=y`: user-attested hardware test passed; canceling a replacement recording restored the prior slot contents.
- [x] Repeat with persistence disabled and verify the slot does not survive reboot: user-attested hardware test passed using a temporary validation build with `CONFIG_ZMK_ZBDM_PERSIST=n`; recording and playback worked during the same boot, and after reboot/power-cycle slot 0 produced no output.
- [x] Review logs for settings save/load errors: maintainer-inspected USB logging from the hardware validation run found no ZBDM/dynamic-macro warnings/errors and no settings save/load warnings/errors. The only settings matches were informational `bt_hci_core: No ID address. App must call settings_load()` boot messages; unrelated ZMK modifier/USB errors were present and are not ZBDM settings save/load failures.

## Physical split behavior evidence

- [x] Both central and peripheral firmware build from the recorded revisions: maintainer validation builds produced the firmware artifacts identified above from ZMK revision `904c9aec8822d79149d42c8a9a77e8828eb08f5a` and ZBDM implementation revision `7a702784bdeab33b3a2775cb756f6227a73417f7`. Build logs recorded central FLASH/RAM `362200 B`/`104132 B` and peripheral FLASH/RAM `280228 B`/`62540 B`.
- [x] The same keymap containing `&zbdm` is accepted by both halves: the release-test Advantage 360 keymap includes the ZBDM behavior DTSI and ZBDM dt-bindings header; built maps show `behavior_zbdm` present on both halves, while `zmk_zbdm_record` is present only on the central/left half and absent from the peripheral/right half as intended.
- [x] Record at least one key from each half and verify playback order: user-attested hardware test passed with a recorded macro containing two keys from the left/central half and one key from the right/peripheral half; playback preserved the recorded cross-half order.
- [x] Disconnect/reconnect the peripheral and repeat recording and playback: user-attested hardware test passed; after disconnecting/reconnecting the right/peripheral half and confirming normal right-half typing resumed, a fresh cross-half macro recorded keys from both halves and playback matched the recorded order.
- [x] Cancel playback while a key is held; verify the host has no stuck key: release-owner waiver for `v0.1.0`. This exact hardware interaction was not repeated because normal playback is too fast to interrupt reliably. Residual risk is accepted for this release because the checked-in `playback-cancel-cleanup` native_sim test covers cleanup for this path, and the related hardware clear-during-playback cleanup test below passed with no stuck key.
- [x] Clear a playing slot; verify the host has no stuck key: user-attested hardware test passed; clearing slot 0 during playback stopped playback, left no stuck key on the host, and left the slot empty.

## Resource and upgrade checks

- [x] Central/peripheral RAM and flash usage from build output: central/left `FLASH 362200 B`, `RAM 104132 B`; peripheral/right `FLASH 280228 B`, `RAM 62540 B`. The release owner accepts these absolute build-output measurements as the `v0.1.0` resource record.
- [x] README compatibility table and installation manifest match this release: `README.md` Quick Start pins ZMK to `904c9aec8822d79149d42c8a9a77e8828eb08f5a` and the module to `v0.1.0`; the compatibility table lists `v0.1.x` against the same required ZMK revision.
- [x] Release notes warn that persisted keystrokes are plaintext: this release note includes the warning below.
- [x] For the namespace migration, old `dynamic_macro/` settings are intentionally ignored and the migration note is present: README documents that old `dynamic_macro/` settings are intentionally not imported. No automatic migration is provided for `v0.1.0`.

## User-facing notes

- Persisted dynamic macros are stored as plaintext/binary keystroke data in Zephyr settings/NVS. Do not record passwords, recovery phrases, or other secrets. Clear dynamic macro slots before sharing, selling, or transferring hardware.

## Known limitations

- Records keyboard and consumer/media HID usage-page events only.
- Playback uses a fixed interval rather than recorded human timing.
- Native simulation does not model physical split forwarding or power-cycle persistence; those are covered by the hardware evidence and `v0.1.0` waiver above.
